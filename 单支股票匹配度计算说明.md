# 单支股票匹配度计算使用说明

## 功能说明

`calculate_single_stock_similarity()` 方法用于计算单支股票与北证50指数的涨跌匹配度。

## 函数签名

```python
def calculate_single_stock_similarity(
    stock_code: str, 
    start_date: str, 
    end_date: str,
    cache: StockDataCache = None, 
    use_cache: bool = True
) -> Dict
```

## 参数说明

- `stock_code` (str): 股票代码，例如 "920000"
- `start_date` (str): 开始日期，格式 YYYYMMDD，例如 "20240101"
- `end_date` (str): 结束日期，格式 YYYYMMDD，例如 "20241231"
- `cache` (StockDataCache, 可选): 缓存管理器，如果为 None 将自动创建
- `use_cache` (bool, 可选): 是否使用缓存，默认 True

## 返回值

返回一个字典，包含以下字段：

```python
{
    "stock_code": "920000",           # 股票代码
    "match_days": 120,                # 匹配天数
    "total_days": 242,                # 总天数
    "match_ratio": 0.4959,            # 匹配率 (0-1之间)
    "details": [                      # 每日详细数据
        {
            "date": "2024-01-02",     # 日期
            "stock_change_pct": 5.83, # 股票涨跌幅 (%)
            "stock_level": 4,         # 股票等级 (1-4)
            "market_level": 4,        # 市场等级 (1-4)
            "matched": True           # 是否匹配
        },
        ...
    ]
}
```

## 等级说明

- 等级 4: 大涨 (涨跌幅 > 1%)
- 等级 3: 小涨 (涨跌幅 0% ~ 1%)
- 等级 2: 小跌 (涨跌幅 -1% ~ 0%)
- 等级 1: 大跌 (涨跌幅 < -1%)

## 使用示例

### 示例1: 基本使用

```python
from stock_analysis_cached import calculate_single_stock_similarity

# 计算股票920000在2024年的匹配度
result = calculate_single_stock_similarity(
    stock_code="920000",
    start_date="20240101",
    end_date="20241231"
)

print(f"匹配率: {result['match_ratio']:.2%}")
print(f"匹配天数: {result['match_days']} / {result['total_days']}")
```

### 示例2: 查看详细数据

```python
# 查看前10天的详细数据
for detail in result['details'][:10]:
    matched = "✓" if detail['matched'] else "✗"
    print(f"{detail['date']}: 股票{detail['stock_change_pct']:+.2f}% vs 市场等级{detail['market_level']} {matched}")
```

### 示例3: 批量计算多支股票

```python
from stock_analysis_cached import calculate_single_stock_similarity, StockDataCache

# 创建缓存管理器（多次调用可复用）
cache = StockDataCache("data")

stock_codes = ["920000", "920001", "920002", "920010"]
results = []

for code in stock_codes:
    result = calculate_single_stock_similarity(
        stock_code=code,
        start_date="20240101",
        end_date="20241231",
        cache=cache  # 复用缓存管理器
    )
    results.append({
        "code": code,
        "match_ratio": result['match_ratio']
    })

# 按匹配率排序
results.sort(key=lambda x: x['match_ratio'], reverse=True)

for i, r in enumerate(results, 1):
    print(f"{i}. {r['code']}: {r['match_ratio']:.2%}")
```

### 示例4: 保存结果到文件

```python
import json

result = calculate_single_stock_similarity("920000", "20240101", "20241231")

# 保存完整结果
with open("result_920000.json", "w", encoding="utf-8") as f:
    json.dump(result, f, ensure_ascii=False, indent=2)

# 只保存匹配的日期
matched_days = [d for d in result['details'] if d['matched']]
print(f"匹配的日期共 {len(matched_days)} 天")
```

### 示例5: 不使用缓存（实时获取数据）

```python
# 如果需要实时最新数据，可以关闭缓存
result = calculate_single_stock_similarity(
    stock_code="920000",
    start_date="20260101",
    end_date="20260114",
    use_cache=False  # 不使用缓存，直接从API获取
)
```

## 运行示例脚本

项目中提供了完整的示例脚本 `calculate_single_stock.py`：

```bash
# 激活虚拟环境（如果使用）
source .venv/bin/activate

# 运行示例
python3 calculate_single_stock.py
```

该脚本演示了：
1. 单支股票匹配度计算
2. 保存结果到JSON文件
3. 批量计算多支股票并排序

## 注意事项

1. **首次运行**: 首次运行需要下载数据，会比较慢（约1-2分钟）
2. **缓存机制**: 启用缓存后，相同时间范围的查询会直接使用本地数据
3. **数据更新**: 如需获取最新数据，请求超出缓存范围的日期即可自动增量更新
4. **API限流**: 批量计算时建议复用 `StockDataCache` 对象，并启用缓存
5. **股票代码**: 北交所股票代码通常为6位数字（如920000），需要以字符串形式传入

## 性能优化建议

- ✅ 启用缓存（默认启用）
- ✅ 批量计算时复用 `StockDataCache` 对象
- ✅ 先计算较短时间范围，确认无误后再扩大范围
- ✅ 利用已有缓存数据，避免重复下载
