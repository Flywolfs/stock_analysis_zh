# 北交所股票分析系统使用说明

## 功能概述

该系统用于分析北交所股票与大盘的相似度，主要功能包括：

1. 获取北交所股票每日涨跌数据
2. 计算北交所整体每日涨跌等级
3. 计算个股每日涨跌等级
4. 计算股票与大盘的匹配率
5. 按匹配率排序展示结果
6. **数据本地缓存和增量更新**

## 涨跌等级分类

- **4 (大涨)**: 涨幅 > 1%
- **3 (小涨)**: 涨幅 0% ~ 1%
- **2 (小跌)**: 跌幅 0% ~ -1%
- **1 (大跌)**: 跌幅 < -1%

## 文件说明

- `stock_analysis.py` - 基础版本（无缓存）
- `stock_analysis_cached.py` - 缓存版本（推荐使用）
- `config.json` - 配置文件
- `data/` - 数据缓存目录
  - `stocks/` - 个股数据缓存（JSON格式）
  - `market/` - 市场数据缓存
  - `metadata.json` - 元数据信息

## 配置文件说明 (config.json)

```json
{
    "data_dir": "data",           // 数据缓存目录
    "start_date": "20240101",     // 分析开始日期 (YYYYMMDD格式)
    "end_date": "20241231",       // 分析结束日期 (YYYYMMDD格式)
    "cache_enabled": true,        // 是否启用缓存
    "max_stocks": 10              // 最大处理股票数量
}
```

## 使用方法

### 方式1: 命令行工具（推荐）

命令行工具支持快速指定参数，无需修改配置文件：

```bash
# 基础用法 - 使用config.json中的配置
python3 analyze.py

# 指定时间范围
python3 analyze.py --start 20240301 --end 20240630

# 指定股票数量和显示前N名
python3 analyze.py --stocks 20 --top 15

# 禁用缓存（强制重新下载）
python3 analyze.py --no-cache

# 组合使用
python3 analyze.py --start 20240601 --end 20240831 --stocks 15 --top 5
```

**命令行参数说明：**
- `--start YYYYMMDD` : 开始日期
- `--end YYYYMMDD` : 结束日期
- `--stocks N` : 最大股票数量
- `--top N` : 显示前N名（默认10）
- `--no-cache` : 禁用缓存

### 方式2: 配置文件方式

#### 1. 安装依赖

```bash
pip install akshare pandas
```

#### 2. 首次运行

首次运行会下载指定日期范围的所有数据：

```bash
python3 stock_analysis_cached.py
```

程序会自动：
- 下载北交所股票数据
- 保存到本地JSON文件
- 计算并显示匹配率排名

#### 3. 修改时间范围

编辑 `config.json` 修改开始和结束日期：

```json
{
    "start_date": "20240601",  // 改为2024年6月1日
    "end_date": "20241231"     // 结束日期不变
}
```

再次运行程序：
- 如果新的时间范围在已缓存的范围内，程序会**直接使用缓存数据**，无需重新下载
- 如果新的时间范围超出已缓存范围，程序**只下载缺失的部分**

#### 4. 增量更新示例

假设当前缓存了 2024-01-01 到 2024-12-31 的数据：

**场景1：查询子区间**
```json
"start_date": "20240301",
"end_date": "20240630"
```
结果：完全使用缓存，速度极快 ⚡

**场景2：扩展到未来**
```json
"start_date": "20240101",
"end_date": "20250131"
```
结果：只下载 2024-12-31 到 2025-01-31 的新数据 📥

**场景3：扩展到过去**
```json
"start_date": "20231201",
"end_date": "20241231"
```
结果：只下载 2023-12-01 到 2024-01-01 的历史数据 📥

## 优势特点

### ✨ 智能缓存
- 首次下载后，数据永久保存在本地
- 自动识别已缓存的数据，避免重复下载
- 大幅减少API请求，加快运行速度

### 📊 灵活配置
- 通过配置文件轻松调整时间范围
- 可以随时修改分析的股票数量
- 支持开启/关闭缓存功能

### 🚀 增量更新
- 只下载缺失的日期段
- 自动合并新旧数据
- 支持向前/向后扩展时间范围

### 💾 数据持久化
- JSON格式存储，易于查看和编辑
- 每只股票独立文件，便于管理
- 包含元数据信息，记录更新时间

## 数据示例

缓存的股票数据格式：

```json
{
  "daily_data": {
    "2024-01-02": 5.83,
    "2024-01-03": -1.83,
    "2024-01-04": 0.47,
    ...
  }
}
```

## 运行输出示例

```
=== 北交所股票分析系统（带缓存版本）===

配置信息:
  数据目录: data
  开始日期: 20240101
  结束日期: 20241231
  缓存启用: True
  最大股票数: 10

正在获取北交所大盘数据...
正在处理第 1/10 只股票: 920000
  股票 920000 使用缓存数据          # ← 使用缓存，无需下载
...

按相似度排序结果:
排名   股票代码       匹配天数       总天数        匹配率       
--------------------------------------------------
1    920001     142        242        0.5868    
2    920000     136        242        0.5620    
...
```

## 注意事项

1. **首次运行时间较长**：需要下载所有数据，请耐心等待
2. **API限制**：避免频繁请求，程序已内置0.5秒延时
3. **数据目录**：不要手动删除data目录，否则需要重新下载
4. **日期格式**：配置文件中的日期必须使用YYYYMMDD格式（如：20240101）

## 常见问题

**Q: 如何清空缓存重新下载？**
A: 删除 `data` 目录即可：`rm -rf data`

**Q: 如何只分析最近3个月的数据？**
A: 修改config.json中的start_date为3个月前的日期

**Q: 缓存文件占用空间大吗？**
A: 非常小，每只股票约10-30KB，10只股票总计约100-300KB

**Q: 可以分析更多股票吗？**
A: 可以，修改config.json中的max_stocks即可，但会增加运行时间

## 技术支持

如有问题，请检查：
1. akshare库是否正确安装
2. 网络连接是否正常
3. config.json格式是否正确
4. data目录的读写权限
