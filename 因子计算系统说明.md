# 北交所股票多因子分析系统

## 📊 系统概述

本系统用于获取北交所股票的完整交易数据，并计算个股与北证50指数的多种相关性因子，用于量化分析和选股。

## 🎯 已实现的因子体系

### 1. **相关性类因子**

| 因子名称 | 说明 | 计算方法 | 取值范围 |
|---------|------|---------|---------|
| pearson_corr | Pearson相关系数 | 个股与北证50涨跌幅的线性相关性 | -1 ~ 1 |
| beta | Beta系数 | 个股相对北证50的系统性风险 | 通常0.5~2 |
| direction_match_rate | 方向一致性 | 同涨同跌的比率 | 0 ~ 1 |

**解读**：
- `pearson_corr > 0.7`：高度正相关，与大盘同步性强
- `beta > 1`：波动大于大盘，高风险高收益
- `beta < 1`：波动小于大盘，防御性股票
- `direction_match_rate > 0.7`：方向高度一致

### 2. **波动性类因子**

| 因子名称 | 说明 | 用途 |
|---------|------|------|
| stock_volatility | 个股波动率 | 衡量个股风险 |
| market_volatility | 市场波动率 | 衡量市场风险 |
| relative_volatility | 相对波动率 | 个股/市场波动率比值 |
| volatility_diff | 波动率差异 | 绝对差值 |
| amplitude_corr | 振幅相关性 | 日内振幅的相关性 |

**解读**：
- `relative_volatility > 1.5`：高波动股票
- `relative_volatility < 0.8`：低波动股票
- `amplitude_corr > 0.5`：日内波动与大盘同步

### 3. **成交量类因子**

| 因子名称 | 说明 | 用途 |
|---------|------|------|
| avg_volume | 平均成交量 | 流动性指标 |
| volume_std | 成交量标准差 | 成交量稳定性 |
| avg_turnover | 平均换手率 | 交易活跃度 |

**解读**：
- `avg_turnover > 10%`：高换手率，交易活跃
- `avg_turnover < 5%`：低换手率，流动性一般

## 💡 建议的扩展因子（待实现）

### 4. **趋势类因子**
- **动量一致性**：N日累计收益率方向是否一致
- **趋势强度差异**：RSI、MACD等技术指标的差异
- **突破同步性**：创新高/新低的同步程度

### 5. **价格形态类因子**
- **K线形态相似度**：K线组合模式的相似性
- **高低点同步性**：创新高/新低的时间重合度
- **支撑压力位相关性**：关键价格位的相关性

## 🚀 使用方法

### 1. 获取单支股票完整数据

```python
from stock_analysis_cached import fetch_and_cache_full_stock_data, StockDataCache

cache = StockDataCache("data")

# 获取股票920000的完整交易数据
df = fetch_and_cache_full_stock_data(
    stock_code="920000",
    start_date="20240101",
    end_date="20241231",
    cache=cache,
    use_cache=True
)

print(f"获取到 {len(df)} 条数据")
print(df.columns.tolist())
print(df.head())
```

**返回的DataFrame包含字段**：
- `date`: 日期
- `open`: 开盘价
- `high`: 最高价
- `low`: 最低价
- `close`: 收盘价
- `volume`: 成交量
- `amount`: 成交额
- `amplitude`: 振幅 (%)
- `pct_chg`: 涨跌幅 (%)
- `change`: 涨跌额
- `turnover`: 换手率 (%)

### 2. 批量获取所有股票数据

```python
from stock_analysis_cached import fetch_all_bj_stocks_full_data

# 获取所有北交所股票的数据（耗时较长）
all_data = fetch_all_bj_stocks_full_data(
    start_date="20240101",
    end_date="20241231",
    max_stocks=None,  # None表示全部股票，可设置为数字限制数量
    use_cache=True
)

print(f"成功获取 {len(all_data)} 支股票的数据")

# 访问某支股票的数据
df_920000 = all_data['920000']
```

### 3. 计算多因子

运行示例脚本：

```bash
# 激活虚拟环境
source .venv/bin/activate

# 运行因子计算示例
python3 fetch_full_data_example.py
```

该脚本会：
1. 获取北证50指数数据
2. 获取个股完整交易数据
3. 计算相关性、波动性、成交量因子
4. 保存结果到 `factors_analysis_result.csv`

### 4. 自定义因子计算

```python
import numpy as np
import pandas as pd

def calculate_custom_factor(stock_df, market_df):
    """自定义因子计算"""
    # 合并数据
    merged = pd.merge(
        stock_df[['date', 'pct_chg']],
        market_df[['date', 'pct_chg']],
        on='date',
        suffixes=('_stock', '_market')
    )
    
    # 计算你的自定义因子
    # 例如：计算20日滚动相关系数
    rolling_corr = merged['pct_chg_stock'].rolling(20).corr(merged['pct_chg_market'])
    
    return {
        'avg_rolling_corr': rolling_corr.mean(),
        'max_rolling_corr': rolling_corr.max(),
        'min_rolling_corr': rolling_corr.min()
    }
```

## 📁 数据存储结构

```
data/
├── full_stocks/              # 完整交易数据缓存
│   ├── 920000_full.json
│   ├── 920001_full.json
│   └── ...
├── stocks/                   # 涨跌幅数据缓存（原有）
│   ├── 920000.json
│   └── ...
├── market/                   # 市场数据缓存
│   └── market_data.json
└── metadata.json             # 元数据
```

## 📈 示例分析结果

以2024年全年数据为例：

| 股票代码 | Pearson相关 | Beta | 方向一致率 | 相对波动率 | 平均换手率 |
|---------|------------|------|-----------|-----------|-----------|
| 920000 | 0.5387 | 0.86 | 72.73% | 1.60 | 5.84% |
| 920001 | 0.6151 | 1.09 | 73.14% | 1.77 | 9.51% |
| 920002 | 0.1755 | 0.81 | 73.29% | 5.38 | 21.79% |

**分析**：
- **920000**：中等相关性，Beta<1，属于防御型股票
- **920001**：较高相关性，Beta≈1，与大盘同步
- **920002**：低相关性，高波动，高换手，属于投机型股票

## 🔍 因子应用场景

### 1. **选股策略**
- 寻找高Beta股票：牛市中放大收益
- 寻找低Beta股票：熊市中防御
- 寻找高相关股票：跟随大盘趋势
- 寻找低相关股票：分散风险

### 2. **配对交易**
- 寻找高相关但当前价差较大的股票对
- 利用相关性回归进行套利

### 3. **风险管理**
- 根据Beta系数调整仓位
- 根据波动率设置止损位
- 根据相关性构建投资组合

### 4. **市场情绪判断**
- 观察整体相关性变化：相关性升高说明市场趋同
- 观察Beta分布：高Beta股票受青睐说明风险偏好提升

## ⚙️ 性能优化

1. **启用缓存**（默认启用）
   - 完整数据缓存在 `data/full_stocks/` 目录
   - 支持增量更新，避免重复下载

2. **批量处理**
   - 复用 `StockDataCache` 对象
   - 每10支股票暂停2-5秒，防止API限流

3. **分批次获取**
   ```python
   # 分3批获取全部287支股票
   batch1 = fetch_all_bj_stocks_full_data("20240101", "20241231", max_stocks=100)
   # 休息几分钟
   batch2 = fetch_all_bj_stocks_full_data("20240101", "20241231", max_stocks=200)
   # 休息几分钟
   batch3 = fetch_all_bj_stocks_full_data("20240101", "20241231", max_stocks=None)
   ```

## 📝 注意事项

1. **首次运行**：获取全部287支股票的完整数据需要约1-2小时
2. **数据质量**：部分股票可能存在缺失数据（未上市期间）
3. **API限制**：建议启用缓存，避免频繁请求
4. **数据复权**：当前使用后复权数据（adjust="hfq"）
5. **时区问题**：所有日期为交易日，不包含非交易日

## 🎓 进阶使用

### 构建多因子评分模型

```python
def calculate_multi_factor_score(factors_df):
    """
    多因子评分模型
    """
    # 标准化因子（Z-score）
    factors_normalized = factors_df.copy()
    for col in ['pearson_corr', 'beta', 'direction_match_rate']:
        mean = factors_df[col].mean()
        std = factors_df[col].std()
        factors_normalized[col + '_z'] = (factors_df[col] - mean) / std
    
    # 加权求和（权重可自定义）
    weights = {
        'pearson_corr_z': 0.4,
        'beta_z': 0.3,
        'direction_match_rate_z': 0.3
    }
    
    factors_normalized['score'] = sum(
        factors_normalized[col] * weight 
        for col, weight in weights.items()
    )
    
    # 排序
    factors_normalized = factors_normalized.sort_values('score', ascending=False)
    
    return factors_normalized
```

## 📚 相关文件

- [`stock_analysis_cached.py`](file:///home/zhangchi/Documents/self_stock_analysis/stock_analysis_cached.py) - 核心功能实现
- [`fetch_full_data_example.py`](file:///home/zhangchi/Documents/self_stock_analysis/fetch_full_data_example.py) - 使用示例
- [`factors_analysis_result.csv`](file:///home/zhangchi/Documents/self_stock_analysis/factors_analysis_result.csv) - 因子计算结果示例
