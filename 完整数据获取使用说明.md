# 完整股票数据获取功能使用说明

## 功能概述

系统提供了获取北交所股票完整交易数据的功能，支持：
- 自动使用股票上市日期到今天的完整历史数据
- 手动指定日期范围获取部分数据
- 增量更新机制，避免重复下载
- 自动保存股票基本信息（证券简称、所属行业、地区等）

## 核心函数

### 1. `fetch_and_cache_full_stock_data()` - 单只股票数据获取

```python
from stock_analysis_cached import fetch_and_cache_full_stock_data, StockDataCache

cache = StockDataCache("data")

# 方式1：不指定日期，自动获取从上市日期到今天的完整数据
df = fetch_and_cache_full_stock_data(
    stock_code="920000",
    use_cache=True
)

# 方式2：指定日期范围获取部分数据
df = fetch_and_cache_full_stock_data(
    stock_code="920000",
    start_date="20240101",
    end_date="20241231",
    use_cache=True
)
```

**返回的DataFrame包含字段：**
- `date`: 日期
- `open`: 开盘价
- `high`: 最高价
- `low`: 最低价
- `close`: 收盘价
- `volume`: 成交量
- `amount`: 成交额
- `amplitude`: 振幅
- `pct_chg`: 涨跌幅(%)
- `change`: 涨跌额
- `turnover`: 换手率

### 2. `fetch_all_bj_stocks_full_data()` - 批量获取所有股票

```python
from stock_analysis_cached import fetch_all_bj_stocks_full_data

# 方式1：获取所有股票的完整历史数据（推荐首次使用）
all_data = fetch_all_bj_stocks_full_data(
    max_stocks=None,  # None表示全部288只股票
    use_cache=True
)

# 方式2：获取前10只股票的完整历史数据（用于测试）
all_data = fetch_all_bj_stocks_full_data(
    max_stocks=10,
    use_cache=True
)

# 方式3：指定日期范围批量获取
all_data = fetch_all_bj_stocks_full_data(
    start_date="20240101",
    end_date="20241231",
    max_stocks=10,
    use_cache=True
)
```

**返回格式：**
```python
{
    '920000': DataFrame,  # 安徽凤凰的完整数据
    '920001': DataFrame,  # 纬达光电的完整数据
    ...
}
```

## 数据存储结构

每只股票的数据保存在 `data/full_stocks/{股票代码}_full.json`：

```json
{
  "stock_code": "920000",
  "last_update": "2026-01-18 00:25:26",
  "证券简称": "安徽凤凰",
  "总股本": 91680000,
  "流通股本": 48306300,
  "上市日期": "2020-12-23",
  "所属行业": "汽车制造业",
  "地区": "安徽省",
  "报告日期": "2026-01-16",
  "data": [
    {
      "date": "2020-12-23",
      "open": 12.88,
      "close": 13.37,
      ...
    },
    ...
  ]
}
```

## 使用场景

### 场景1：首次获取完整历史数据

```python
from stock_analysis_cached import fetch_all_bj_stocks_full_data

print("开始下载北交所所有股票的完整历史数据...")

# 一次性下载所有股票从上市日期到今天的数据
all_data = fetch_all_bj_stocks_full_data(
    max_stocks=None,  # 全部股票
    use_cache=True
)

print(f"成功获取 {len(all_data)} 只股票的数据")
```

### 场景2：日常增量更新

```python
from stock_analysis_cached import fetch_all_bj_stocks_full_data

# 每天运行，自动下载新增数据
# 系统会自动检测缓存，只下载新增日期的数据
all_data = fetch_all_bj_stocks_full_data(
    max_stocks=None,
    use_cache=True
)
```

### 场景3：获取特定时间段数据

```python
from stock_analysis_cached import fetch_all_bj_stocks_full_data

# 获取2024年的数据用于回测
all_data = fetch_all_bj_stocks_full_data(
    start_date="20240101",
    end_date="20241231",
    max_stocks=None,
    use_cache=True
)
```

### 场景4：结合因子计算

```python
from stock_analysis_cached import fetch_and_cache_full_stock_data, StockDataCache

cache = StockDataCache("data")

# 获取个股和北证50的完整数据
stock_df = fetch_and_cache_full_stock_data("920000", use_cache=True)
market_df = fetch_and_cache_full_stock_data("899050", use_cache=True)

# 计算相关性因子
from fetch_full_data_example import calculate_correlation_factor
factors = calculate_correlation_factor(stock_df, market_df)

print(f"Pearson相关系数: {factors['pearson_corr']}")
print(f"Beta系数: {factors['beta']}")
print(f"方向一致性: {factors['direction_match_rate']}")
```

## 性能优化

### 反爬机制
- 每次请求随机延迟 0.5-2 秒
- 失败自动重试 3 次（递增等待 2/4/6 秒）
- 每处理 10 只股票暂停 2-5 秒

### 缓存策略
- 自动检测已下载的数据范围
- 只下载缺失的时间段
- 增量更新，避免重复下载

### 建议
- 首次下载全部股票需要较长时间（约1-2小时）
- 建议先用 `max_stocks=10` 测试
- 日常更新速度很快（几分钟内完成）

## 注意事项

1. **日期格式**：所有日期参数使用 `YYYYMMDD` 格式（如 `20240101`）
2. **缓存目录**：数据保存在 `data/full_stocks/` 目录
3. **网络稳定性**：下载大量数据时确保网络稳定
4. **数据完整性**：系统会自动处理股票上市前的数据请求

## 常见问题

**Q: 如何判断数据是否最新？**  
A: 查看 JSON 文件中的 `last_update` 字段。

**Q: 如何重新下载某只股票的数据？**  
A: 删除对应的 JSON 文件，然后重新运行获取函数。

**Q: 为什么有些股票没有数据？**  
A: 可能是新上市股票在指定日期范围内还未上市。

**Q: 数据存储占用多少空间？**  
A: 每只股票约 50-200KB，全部 288 只股票约 20-50MB。
